# behat.yml
default:
  suites:
    calculatrice_binaire:
      paths:
        - features/binaryCalcul.feature
      contexts:
        - BinaryCalculContext
    end_to_end:
      paths:
        - features/endtoend.feature
      contexts:
        - EndToEndContext